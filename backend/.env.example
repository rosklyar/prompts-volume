# Environment variables template for prompts-volume service

# ========================================
# DataForSEO API Configuration
# ========================================
# Get your credentials from https://app.dataforseo.com/
DATAFORSEO_USERNAME=your_username_here
DATAFORSEO_PASSWORD=your_password_here

# Optional: DataForSEO API endpoint URL (default shown)
# DATAFORSEO_BASE_URL=https://api.dataforseo.com/v3/dataforseo_labs/google/ranked_keywords/live

# Optional: Keywords per API request (default: 1000, max: 1000)
# DATAFORSEO_BATCH_SIZE=1000

# Optional: Maximum total keywords to fetch across all paginated requests (default: 10000)
# DATAFORSEO_MAX_TOTAL=10000

# Optional: API request timeout in seconds (default: 30.0)
# DATAFORSEO_TIMEOUT=30.0


# ========================================
# OpenAI API Configuration
# ========================================
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Model for business domain detection (default: gpt-4o-mini)
# DOMAIN_DETECTION_MODEL=gpt-4o-mini

# Optional: Model for topics generation (default: gpt-4o-mini)
# TOPICS_GENERATION_MODEL=gpt-4o-mini

# Optional: Similarity threshold for matching LLM-generated topics to DB topics (default: 0.9, range: 0.0-1.0)
# Higher values = stricter matching (only very similar topics match)
# TOPICS_PROVIDER_SIMILARITY_THRESHOLD=0.9

# Optional: Model for prompts generation (default: gpt-4o-mini)
# PG_OPENAI_MODEL=gpt-4o-mini


# ========================================
# Clustering Configuration (HDBSCAN)
# ========================================
# Optional: Minimum cluster size - smaller values create more granular clusters (default: 5)
# CLUSTERING_MIN_CLUSTER_SIZE=5

# Optional: Minimum samples in neighborhood - higher values create more conservative clusters (default: 5)
# CLUSTERING_MIN_SAMPLES=5

# Optional: Distance threshold for cluster merging (default: 0.0)
# CLUSTERING_CLUSTER_SELECTION_EPSILON=0.0


# ========================================
# Topic Relevance Filter Configuration
# ========================================
# Optional: Minimum cosine similarity for keyword-to-topic relevance (default: 0.7, range: 0.0-1.0)
# TOPIC_FILTER_SIMILARITY_THRESHOLD=0.7

# Optional: Minimum percentage of relevant keywords to keep cluster (default: 0.5, range: 0.0-1.0)
# TOPIC_FILTER_MIN_RELEVANT_RATIO=0.5


# ========================================
# Database Configuration
# ========================================
# PostgreSQL connection URL (asyncpg driver for async support)
# Format: postgresql+asyncpg://username:password@host:port/database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/prompts

# Optional: Enable SQL query logging (default: false)
# DATABASE_ECHO=false
